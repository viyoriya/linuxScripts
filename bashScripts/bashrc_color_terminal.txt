check the hidden files in this folder

run the runInitScript.sh 

or manualy add these lines 


cp .bashrc_color_terminal /root/

add this in /root/.bashrc

if [ -f ~/.bashrc_color_terminal ]; then
    . ~/.bashrc_color_terminal
fi
